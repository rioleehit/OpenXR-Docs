services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: khronosgroup/docker-images:openxr.202110
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

